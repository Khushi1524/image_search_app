import { render } from "@testing-library/react"

const Imagecard = (images) =>{
    render(
        <div className="imageStructure">
          <div>
          <img src={} alt="uplash image" />
          </div>
        </div>
    )
}

export default Imagecard;